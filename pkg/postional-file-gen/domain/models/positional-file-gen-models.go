package models

// where we put batch_id creation logic it should be in the main file.
// wether further checks are necessary to be done in the package or we can do this else where

// type DataMapper interface {

// 	DataMapper DataMapperObj ;
// }
